/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},  // It's a good practice to include autoprefixer
  },
};

export default config;
